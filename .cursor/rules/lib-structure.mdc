---
description: Storage layout and library structure for facets.
globs:
  - "libs/**/*.sol"
mode: always
---

# Lib + Storage Structure

- Each facet with internal state must use a storage lib:
  - Format: `LibFacetNameStorage.sol`
  - Layout in struct: `FacetNameStorage`
- Add a unique storage slot:
```solidity
bytes32 internal constant STORAGE_SLOT = keccak256("facetname.storage");
